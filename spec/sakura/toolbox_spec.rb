require 'spec_helper'

describe Sakura::Toolbox do
  it 'has a version number' do
    expect(Sakura::Toolbox::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
